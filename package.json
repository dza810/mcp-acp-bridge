{
  "name": "mcp-acp-bridge",
  "version": "0.1.2",
  "description": "MCP server that bridges MCP to ACP (Agent Client Protocol) supporting gemini-cli and opencode",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "mcp-acp-bridge": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/dza810/mcp-acp-bridge.git"
  },
  "keywords": [
    "mcp",
    "acp",
    "agent-client-protocol",
    "model-context-protocol",
    "gemini",
    "opencode",
    "ai"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "vitest": "^4.1.9"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
